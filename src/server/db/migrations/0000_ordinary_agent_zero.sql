CREATE TABLE `users` (
	`id` text PRIMARY KEY NOT NULL,
	`firstName` text NOT NULL,
	`lastName` text NOT NULL,
	`bio` text NOT NULL,
	`favouriteFood` text NOT NULL,
	`favouriteAnimal` text NOT NULL
);
